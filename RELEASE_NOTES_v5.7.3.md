# v5.7.3 — Live Deployment Status

The individual display page now refreshes deployment state every five seconds
without a full browser reload.

It updates:

- the top software-status card;
- deployment progress;
- version-verification status;
- the deployment timeline;
- the displayed live version.

It also refreshes immediately when the browser tab becomes visible or regains
focus.
