# v4.5.1 — Safe Fleet Operations installer

- Registers Fleet Operations without depending on a specific navigation layout.
- Adds the menu link only when a safe insertion point exists.
- Leaves the page directly reachable at `/fleet-operations` otherwise.
- Validates both the base template and Fleet Operations template.
