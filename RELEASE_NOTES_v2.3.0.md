# v2.3.0 Reliability Foundation

- Adds dataclass-based payload validation.
- Adds SQLite-backed operational event history while retaining the text event log.
- Adds startup diagnostics for writable runtime paths, SQLite, and rclone.
- Adds configurable playlist insertion policies.
- Adds automated unittest coverage for payload validation and insertion ordering.
- Adds one-command compile and test scripts.
