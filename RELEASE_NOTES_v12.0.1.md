# Church Display Platform v12.0.1

## Content Deployment layout hotfix

- Corrects the Content Deployments template so it extends the shared `base.html` layout.
- Restores the application shell, dark theme, navigation, page-specific deployment styling, and command controls.
- Adds a regression test to prevent pages from extending the shell partial directly.

No deployment workflow or data format changes are included.
