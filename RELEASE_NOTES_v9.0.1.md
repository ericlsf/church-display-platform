# v9.0.1 — Shell Stabilization

- Replaces the brittle v8.1 string assertion with resilient checks.
- Updates the legacy v7 navigation test to validate the v9 application shell.
- Adds a v9 regression test confirming only one shell is loaded.
- Confirms exactly one sidebar, top bar, and breadcrumb row.
- No production UI behavior changes.
